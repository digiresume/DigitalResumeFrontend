const button = document.querySelector('.btn')
const form   = document.querySelector('.form')

button.addEventListener('click', function Design() {
   form.classList.add('form--no') 
});

// export default Design;
